apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: expense
spec:
  selector:
    component: backend
    project: expense
    tier: app
  ports:
  - protocol: TCP
    port: {{ .Values.service.port }} #service-port
    targetPort: {{ .Values.service.port }} #target-port