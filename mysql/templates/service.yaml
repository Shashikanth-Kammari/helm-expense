apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: expense
spec:
  selector:
    component: mysql
    project: expense
    tier: db
  ports:
  - protocol: TCP
    port: {{ .Values.deployment.port }} #service-port
    targetPort: {{ .Values.deployment.port }} #target-port